<p>{{survey.question}}</p>

<form action="/answer" method="post">
    {{#each survey.valid_responses}}
      <input type='radio' name='response' value={{this}}>{{this}}<br>
    {{/each}}
    <div>
        <input type="submit" value="Answer"/>
    </div>
</form>

{{#if creator}}
    <p>created by {{creator.username}}</p>
{{else}}
    <p>creator not found</p>
{{/if}}
